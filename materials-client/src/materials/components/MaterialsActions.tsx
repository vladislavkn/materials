import Button from "@/shared/ui/Button/Button";
import { FC } from "react";

const MaterialsActions: FC = () => {
  return (
    <>
      <Button variant="secondary">Account</Button>
      <Button>Add Material</Button>
    </>
  );
};

export default MaterialsActions;
